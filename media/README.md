# Media Assets

The screenshots in this directory are App previews for the Nomily App repository.

Product photography, screenshots, logos, product names, and other brand assets in this directory are not covered by the repository's source-code license. Their inclusion does not grant trademark or commercial reuse rights.

`app-recordings.png`, `app-asr-providers.png`, and `app-llm-providers.png` are App-preview screenshots. `app-llm-providers.png` is sanitized: its API-key fields show the synthetic text `API key hidden`.

`apple-watch-recording.png` shows the Nomily Apple Watch recording screen.
`apple-watch-install-nomily-ai.png` shows the iPhone Watch app's Available Apps
list with Nomily AI ready to install. It is an installation guide image, not a
claim that every listed third-party app is associated with Nomily.

App screenshots must use synthetic or non-sensitive data. Do not add images containing API keys, Wi-Fi credentials, device serial numbers, Bluetooth MAC addresses, peripheral UUIDs, recordings, transcripts, account details, or private infrastructure.
